# Splice

## What worked
- Hub app SKU `splice--splice`: Homebrew cask `https://formulae.brew.sh/api/cask/splice.json` → **5.4.12**; installer URL embeds version; S3 HEAD 200.

## Confidence raise 7 (2026-09-10)
- Splice hub **5.4.12**: manufacturer CDN `desktop.splice.com/.../splice-5.4.12-mac-aarch64.zip` HEAD 200 → confidence **90** (amber→green).
