# airwindows

## universe-expand-3 version chip (2026-09-10 ~2:50 AM PT (expand-3 version chip))
- **Freeware:** all `is_freeware=1` (MIT / free downloads on airwindows.com).
- **Strategy:** Do **not** invent one global version for ~514 algorithm plugs. Standalone zips (`Air4.zip`, …) and platform packs (`64 Bit Windows VSTs.zip`) lack semver in filenames.
- **Airwindows Consolidated only:** GitHub `baconpaul/airwin2rack` release tag `DAWPlugin` assets `2026-09-05-2a6d1c0` → version `2026-09-05-2a6d1c0` @90 (`installer-filename`). Linked from airwindows.com Consolidated Download.
- **Sparse KVR:** SoftGate / DrumSlam exact product pages @60 (`1.0`). Rejected bad fuzzy maps (Air←Consolidated, Desk4←Desk, Logical4←Logical).
- Remaining algorithms: leave without version until per-plugin manufacturer/GitHub evidence exists.

## gaps-mop-airwindows-policy (2026-09-10 ~3:05 AM PT) — POLICY LOCK
- **512** individual algorithms remain **intentionally unversioned** (`identity_kind=plugin`, `is_freeware=1`, explanatory `notes_for_user`).
- Electron must show **Airwindows Consolidated** version / notes / isFreeware — **do NOT invent per-SKU versions** from Consolidated date-shortsha.
- Do **not** mass-reclass individuals as `suite_component` while standalone AU/VST zips still ship on airwindows.com; prefer notes + portal to Consolidated.
- Only versioned SKU: `airwindows--airwindows-consolidated` = `2026-09-05-2a6d1c0`. SoftGate/DrumSlam weak KVR 1.0 outliers stay; do not expand pattern.
