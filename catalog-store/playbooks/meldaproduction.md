# MeldaProduction

## What worked
- Shared **kernel version** on https://www.meldaproduction.com/downloads (`installer version: 02.30 · kernel version: 17.10.01`).
- changes page: all plugins share kernel version numbers.
- Universe mop-1: applied kernel **17.10.01** @92 to all remaining true plugins (+88).
- Bundles (`*Bundle`) → `identity_kind=bundle`; MDrummer/Power* content → `soundset`.

## Caveats
- `api/products` is price CSV only (no versions).
- Offline zips on develop.meldaproduction.com may lag public downloads kernel string — prefer downloads page.
