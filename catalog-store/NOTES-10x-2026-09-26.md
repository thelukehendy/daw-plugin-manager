# NOTES-10x 2026-09-26 — maintenance cadence (12h, TIER-1-ONLY)

## Daily push 2026-09-26 ~06:00 PT (cron `daw-catalog-daily-push`)

- **Shipped:** mfrs **664** (+0), plugins **9,506** (+0), accepted **5,668** (+2); green **3,968** (+0), amber **352** (+0), yellow **1,348** (+2). Deltas measured against the last shipped state (`5f7d5b4`, which included the 09-25 advisory data fixes + the 10:18 chip's PA freshness refreshes).
- **36 researched** across the 09-25 2218 chip (**2 promoted**, 28 observed, 6 skipped). 30 export rows changed:
  - **Version bumps (2):** tracktion--waveform-free-13 and tracktion--waveform-pro-13: versionless → **13.3.13 @60** — KVR product-page verwin oracle (Free page text "Product / Version / 13.3.13"; Pro page `id="verwin">13.3.13` with exact-title identity check). Two agreeing KVR pages; standing Tracktion KVR@60 practice. Both enter as yellow (KVR-only evidence).
  - **Freshness-only (28):** 27 Tracktion instruments (KVR verwin = stored, exact title identity each page) + renoise--redux 1.4.4 @60 — all held, `versionVerifiedAt` refreshed.
  - 6 principled skips: arturia--software-center (hub_app, no public version), steinberg--elicenser-control-center (service closed 2025-05-20), 4 Tracktion soundsets (intentionally unversioned).
- **Freshness:** PA slice 2 (47 rows) shipped via the 09-25 advisory commits before this push; SSL auto-series Zendesk watch — no 1.0.43-class re-release; Waveform 13.5.x authoritative-channel watch continues (shadow versions on KVR forum + piracy mirrors only — not evidence).
- **Mechanics:** backup `backups/2026-09-26/` + manifest (manifest commit_sha = `5f7d5b4`, the commit the snapshot shipped with); export re-run; `catalog/catalog.json` re-synced (structural check OK, schemaVersion 3); dashboard rebuilt; commit via Git Data API (ghapi.py), remote ref verified, no force-push; v2 feed pointer published; CDN verification silent-pass.
- **Reconcile note:** remote had advanced (Cursor PR #3 + #11 merges — app UI rewrite, all `src/` + `advisory/`); fast-forwarded local main to `e0733bf8` before building the commit. No overlap with catalog paths.
